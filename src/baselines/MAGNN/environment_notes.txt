python = 3.11.4

Check that the python verison is 3.11.4:
python --verison

if not, you need to download a local verison and use what you downloaded to create your virtual environment

python -m venv ./env

pip install numpy

pip install torch==2.4.0 torchvision==0.19.0 torchaudio==2.4.0 --index-url https://download.pytorch.org/whl/cu124

pip install  dgl -f https://data.dgl.ai/wheels/torch-2.4/cu124/repo.html

pip install scikit-learn
pip install nltk

For the missing nltk resources:
 >> python
import nltk
nltk.download(‘wordnet’)
exit()

had to add 
 >> DGLBACKEND=pytorch
to .bashrc